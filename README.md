# TeamHub - Система управления проектами

## 📋 Описание

TeamHub - это современная система управления проектами с поддержкой SCRUM методологии, Kanban досок и командной работы.

## 🏗️ Архитектура проекта

### Структура файлов

```
src/
├── components/          # React компоненты
│   ├── AuthPage.tsx    # Страница авторизации
│   ├── Board.tsx       # Kanban доска
│   ├── Header.tsx      # Шапка приложения
│   ├── Sidebar.tsx     # Боковое меню
│   └── ...
├── hooks/              # React хуки
│   ├── useAuth.ts      # Авторизация
│   ├── useBoard.ts     # Управление досками
│   ├── useProjects.ts  # Управление проектами
│   └── ...
├── lib/                # Библиотеки и утилиты
│   ├── database.ts     # Локальная база данных
│   └── supabase.ts     # Supabase клиент
├── types/              # TypeScript типы
│   ├── Task.ts         # Типы задач
│   ├── Project.ts      # Типы проектов
│   └── ...
└── utils/              # Вспомогательные функции
```

## 🗄️ База данных

### Схема данных

#### Основные сущности:

1. **User** - Пользователи системы
   - `id` - UUID пользователя
   - `email` - Email для входа
   - `full_name` - Отображаемое имя
   - `role` - Роль в системе

2. **Project** - Проекты
   - `id` - UUID проекта
   - `title` - Название
   - `description` - Описание
   - `status` - Статус (active, completed, paused, archived)
   - `owner_id` - Владелец проекта

3. **Task** - Задачи
   - `id` - UUID задачи
   - `title` - Название
   - `status` - Статус (todo, inprogress, inprogress2, done)
   - `priority` - Приоритет (low, medium, high)
   - `project_id` - Проект
   - `assignee_id` - Исполнитель

4. **ProjectMember** - Участники проектов
   - `project_id` - Проект
   - `user_id` - Пользователь
   - `role` - Роль (owner, member, observer)

### Хранение данных

Данные хранятся в localStorage браузера с префиксом `teamhub_`:
- `teamhub_users` - Глобальные пользователи
- `teamhub_projects_{userId}` - Проекты пользователя
- `teamhub_tasks_{userId}` - Задачи пользователя
- `teamhub_documents_{userId}` - Документы пользователя

## 🚀 Запуск проекта

### Требования
- Node.js 18+
- npm или yarn

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd teamhub

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Supabase (опционально)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Telegram Bot (опционально)
VITE_TELEGRAM_BOT_TOKEN=your_bot_token
```

## 🔧 Конфигурация

### Режимы работы

1. **Локальный режим** (по умолчанию)
   - Данные хранятся в localStorage
   - Не требует внешних сервисов
   - Подходит для демо и разработки

2. **Supabase режим** (при наличии конфигурации)
   - Данные хранятся в Supabase
   - Поддержка многопользовательского режима
   - Требует настройки Supabase проекта

### Настройка Supabase (опционально)

1. Создайте проект в [Supabase](https://supabase.com)
2. Выполните миграции из папки `supabase/migrations/`
3. Добавьте переменные окружения
4. Настройте RLS политики

## 📱 Функциональность

### Основные возможности

- ✅ **Авторизация** - Регистрация и вход по email/паролю
- ✅ **Проекты** - Создание, редактирование, архивирование
- ✅ **Задачи** - Kanban доска с drag & drop
- ✅ **Команда** - Приглашение участников, роли
- ✅ **Документы** - Шаблоны и генерация документов
- ✅ **Аналитика** - Статистика и отчеты
- ✅ **Настройки** - Темы, уведомления, экспорт данных

### Статусы задач

- **todo** - К выполнению (холодильник)
- **inprogress** - Сделать
- **inprogress2** - В работе
- **done** - Выполнено

### Роли в проекте

- **owner** - Владелец (полный доступ)
- **member** - Участник (создание/редактирование задач)
- **observer** - Наблюдатель (только просмотр)

## 🎨 UI/UX

### Дизайн система

- **Цвета**: Синий (#3B82F6) как основной
- **Шрифты**: Inter для интерфейса
- **Компоненты**: Tailwind CSS
- **Иконки**: Lucide React

### Адаптивность

- 📱 **Мобильные устройства** - Полная поддержка
- 💻 **Планшеты** - Адаптивная сетка
- 🖥️ **Десктоп** - Оптимизированный интерфейс

## 🔒 Безопасность

### Локальный режим
- Данные хранятся только в браузере пользователя
- Нет передачи данных на сервер
- Автоматическая очистка при выходе

### Supabase режим
- Row Level Security (RLS)
- Шифрование данных в транзите
- Аутентификация через JWT токены

## 📊 Мониторинг

### Логирование

Все операции логируются в консоль браузера:
- 🔐 **AUTH** - Операции авторизации
- 📁 **PROJECT** - Работа с проектами  
- 📋 **TASK** - Управление задачами
- 👥 **TEAM** - Командные операции
- 🔍 **SEARCH** - Поиск и фильтрация

### Отладка

Включите консоль разработчика (F12) для просмотра детальных логов всех операций.

## 🚀 Развертывание

### Netlify (рекомендуется)

```bash
# Сборка проекта
npm run build

# Развертывание через Netlify CLI
netlify deploy --prod --dir=dist
```

### Другие платформы

- **Vercel**: `vercel --prod`
- **GitHub Pages**: Настройте GitHub Actions
- **Docker**: Используйте Nginx для статических файлов

## 🤝 Разработка

### Добавление новых функций

1. Создайте компонент в `src/components/`
2. Добавьте типы в `src/types/`
3. Создайте хук в `src/hooks/` при необходимости
4. Обновите базу данных в `src/lib/database.ts`

### Стиль кода

- TypeScript для типизации
- ESLint для проверки кода
- Prettier для форматирования
- Комментарии на русском языке

## 📝 Лицензия

MIT License - свободное использование и модификация.

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Убедитесь в правильности конфигурации
3. Очистите localStorage при необходимости
4. Создайте issue в репозитории

---

**TeamHub** - Управляйте проектами эффективно! 🚀